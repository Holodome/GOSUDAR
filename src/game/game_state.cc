#include "game/game_state.hh"

GameState::GameState() {
    // memset(this, 0, sizeof(*this));
}

void GameState::init() {
    // ui.font = new Font("c:/windows/fonts/arial.ttf", 32);
}

void GameState::update() {
    // ui.begin_frame();
    
    // game->renderer.text(Vec2(20, 100), Vec4(1, 1, 0, 1), "Hello", ui.font, 2.0f);
    
    // game->renderer.view_proj = Mat4x4::ortographic_2d(0, game->input.winsize.x, game->input.winsize.y, 0);
    // ui.window("Window", Rect(0, 0, 100, 100));
    // ui.textf("FPS: %.2f", 1.0f / game->input.dt);
    // if (ui.button("some button")) {
    //     printf("hello\n");
    // }
    // ui.window_end();    
        
    // ui.end_frame();
}